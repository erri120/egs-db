<!doctype html>
<html lang="en">
<head>
    {{> header }}
</head>
<body>
    <ul>
    {{#each data}}
        <li><a href="{{ ../siteData.baseURL }}/namespaces/{{ @key }}/index.html">{{@key}}</a> ({{ . }})</li>
    {{/each}}
    </ul>
{{> footer }}
</body>
</html>
